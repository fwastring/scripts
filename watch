#!/usr/bin/env bash
shopt -s nocasematch

dir=~/videor/to-watch/

(eza --no-quotes "$dir") |  rofi -dmenu -i -l 10 -p watch: | while IFS= read -r selection; do
    [[ -n "$selection" ]] && mpv "$dir$selection"
done
